# This is a custom ubuntu image with ssh
# NOTE - FROM should always be the first line
FROM ubuntu:xenial
MAINTAINER abc <abc@example.com>
RUN apt-get update
RUN apt-get install telnet openssh-server -y
